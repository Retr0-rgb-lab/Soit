{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimal permissions for Wave 1 shell + bootstrap",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "allow-get-bootstrap-state",
    "allow-get-workspace-snapshot",
    "allow-select-vault",
    "allow-ping"
  ]
}
