# PEL-149 Plan 03: LeftRail IA + integrate FocusOrbit

> **For agentic workers:** Ownership; commit `(pel-149-03)`.  
> **Scheme:** P0 + wire P1/P2  
> **工作目录:** assigned worktree  
> **Wave:** 1 parallel but **merge last** (touches LeftRail). If FocusOrbit/orbitLayout missing, add thin placeholders that match imports so build passes, then real components from 01/02 replace on merge.

## Mission

1. P0 IA cleanup on left rail  
2. Embed FocusOrbit under 活线  
3. Accordion-ish sections (CSS, no GSAP)  
4. Collapsed HUD badges  
5. Optional rail width token bump

## File ownership (ONLY)

| Path | Action |
|------|--------|
| `src/components/shell/LeftRail.tsx` | rewrite structure |
| `src/styles/app.css` | rail sections, accordion, collapsed badges — **rail-related rules only** |
| `src/styles/tokens.css` | `--rail-w` maybe 200→228 |
| `src/components/shell/AGENTS.md` | document new IA if file exists |
| `src/lib/orbitLayout.ts` | import only; create stub if absent |
| `src/components/shell/FocusOrbit.tsx` | import only; create minimal stub if absent |

**Forbidden:** MapStage, LocusPeek logic rewrite, card/*, universe rust, FocusOrbit heavy restyle (02 owns).

## P0 checklist

- [ ] Remove recent padding fill (`nodes` array dump when recent < 5). Empty state OK.
- [ ] 本库 → single compact row
- [ ] 线债: show unread count badge on live root rows when possible; keep expandable debt details collapsed by default
- [ ] Accordion: 活线 default open; 最近/线债 default collapsed (button toggles)
- [ ] Collapsed rail: show live count + unread total badges + map + jump
- [ ] Naming fence intact (本库/活线/移出活线)

## P1 wire

- Under open 活线 section: if `liveIds[0]` or focus root exists, `buildOrbitModel(nodes, focusId)` → `<FocusOrbit model onSelect={open} />`
- Keep minimal live list OR replace list with orbit + unpin control for center
- `open(id)` = focusNode + setMode focus

## Verify

```bash
npm test
npm run build
```

Commit: `feat(shell): (pel-149-03) left rail IA + FocusOrbit host`

## Diff Report

`plan: pel-149-03`
