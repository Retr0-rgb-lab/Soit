# Plan 06: polish + keyboard + docs + verify

> **Spec:** v1.1 §2.9–2.10, §6  
> **工作目录:** `E:\学习软件\Soit`  
> **Wave:** 3

---

### Task 6.1: focus O(1) + keyboard
**Files:** `workspaceStore.ts`, `AppShell.tsx`

- [ ] focusNode patches single node unread
- [ ] Alt+↑↓[] navigation when not typing and mode=focus

### Task 6.2: docs
**Files:** Create `知识库/docs/map-scale-lod.md` (short decision memo)

### Task 6.3: Verify + commit uncommitted shell from prior layout if needed
```bash
npx tsc --noEmit
npx vitest run
npx vite build
```

### Task 6.4: Commit
```bash
git add -A
git commit -m "feat(map): keyboard nav, focus patch, scale lod docs"
```

## Acceptance
- [ ] All §6 checkboxes
- [ ] push branch when clean
