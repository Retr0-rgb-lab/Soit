# Plan E: SKILL.md load + seed + toggle UI

> **For agentic workers:** After A; prefer after C for inject hook; `(plan-e)`.  
> **Spec:** v1.1 §Wave E  
> **Wave:** 2 (parallel with C if file ownership held)

## 0. Mission

Index `vault/.soit/skills/<id>/SKILL.md` on open_universe; enable/disable; seed two skills; inject enabled skill text into chat system context (hook in ChatPort or host); no marketplace; no visual skill editor.

## 1. File ownership

| Path | Action |
|------|--------|
| `src-tauri/src/skills.rs` | **new** index, read, list, set_enabled (meta table or skills_state.json) |
| `src-tauri/src/lib.rs` | mod + commands only |
| `vault seed templates` under `src-tauri/skills_seed/` or write on first open | organize-cards, organize-obsidian |
| `src/lib/host.ts` | listSkills, setSkillEnabled |
| `src/components/shell/SkillsPanel.tsx` | **new** simple list+toggle |
| `src/components/shell/LeftRail.tsx` or AppShell | entry「设置 · 技能」open panel — copy only |
| `src-tauri/src/obsidian.rs` or plugins | `vault/.soit/plugins/README.md` seed on open |
| permissions/capabilities | as needed |

If ChatPort exists: inject in mock/complete system prompt. If not yet: store skills text on snapshot for C to consume.

## 2. Acceptance

- [ ] open_universe creates seed skills if missing
- [ ] toggle persisted
- [ ] no market UI
- [ ] tests for parse SKILL.md frontmatter/body

## 3. Diff Report `"plan":"E"`
