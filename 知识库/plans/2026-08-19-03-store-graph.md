# Plan 03: Store Graph + Rails

> **For agentic workers:** Do NOT modify `src/components/card/**` or `src/components/overlays/**`.  
> **Spec:** v1.1 §2.3, §2.6, W2-A  
> **工作目录:** `E:\学习软件\Soit`  
> **Wave:** 2 · **Depends:** Plan 02 · **Parallel with:** Plan 04

**Goal:** 完整 workspaceStore + 种子 + 左栏探究列表 + 右栏 SVG 节点图；点击换焦；分叉后图与列表一致。

## Global Constraints

- 只写：`src/state/**`, `src/lib/graphLayout.ts`, `src/lib/demoSeed.ts`, `src/components/shell/LeftRail.tsx`, `src/components/shell/RightGraph.tsx`, `src/App.tsx` 仅限组装 graph/rail props（尽量少碰）
- 不改 store 方法名
- 深挖/发散 kind；重生不建节点
- 无 CDN 字体

---

### Task 1: graphLayout + store tests (TDD)

**Files:**
- Create: `src/lib/graphLayout.ts`, `src/state/workspaceStore.test.ts`
- Modify: `src/state/workspaceStore.ts`, `src/lib/demoSeed.ts`
- DevDep: `vitest` if missing

**Interfaces:**
- Consumes: types + store API from Plan 02
- Produces:

```ts
// graphLayout.ts
export function layoutGraph(nodes: InquiryNode[]): InquiryNode[]
// mutates or returns copy with x,y for SVG viewBox 0..200 x 0..300
```

- [ ] **Step 1: 添加 vitest**

```bash
npm install -D vitest jsdom @testing-library/react
```

`vite.config.ts` 加 `test: { environment: "node" }` 或 jsdom。

- [ ] **Step 2: 失败测试 — 分叉不变量**

```ts
// src/state/workspaceStore.test.ts
import { beforeEach, describe, expect, it } from "vitest";
import { useWorkspaceStore } from "./workspaceStore";
import { demoSnapshot } from "../lib/demoSeed";

describe("workspaceStore", () => {
  beforeEach(() => {
    useWorkspaceStore.getState().loadSnapshot(demoSnapshot());
  });

  it("spawnDeepen adds child and focuses it", () => {
    const before = useWorkspaceStore.getState().nodes.length;
    const id = useWorkspaceStore.getState().spawnDeepen("测试");
    const s = useWorkspaceStore.getState();
    expect(s.nodes.length).toBe(before + 1);
    expect(s.focusId).toBe(id);
    const n = s.nodes.find((x) => x.id === id)!;
    expect(n.kind).toBe("deepen");
    expect(n.parentId).toBeTruthy();
  });

  it("regenerateTurn does not add nodes", () => {
    const s0 = useWorkspaceStore.getState();
    const card = s0.focusId;
    const turnId = s0.turnsByCardId[card][0].id;
    const n0 = s0.nodes.length;
    s0.regenerateTurn(turnId);
    expect(useWorkspaceStore.getState().nodes.length).toBe(n0);
  });
});
```

- [ ] **Step 3: 跑测试见红**

```bash
npx vitest run src/state/workspaceStore.test.ts
```

- [ ] **Step 4: 实现/补全 store + demoSeed + layoutGraph** 使测试绿。

- [ ] **Step 5: 再跑测试**

```bash
npx vitest run
```

Expected: PASS

- [ ] **Step 6: Commit**

```bash
git add src/state src/lib/graphLayout.ts src/lib/demoSeed.ts vite.config.ts package.json package-lock.json
git commit -m "feat: workspace store, demo seed, graph layout with tests"
```

---

### Task 2: LeftRail + RightGraph UI

**Files:**
- Modify: `src/components/shell/LeftRail.tsx`, `RightGraph.tsx`, maybe `AppShell.tsx`

- [ ] **Step 1: LeftRail**  
  显示 logo、nav 占位、探究列表（store.nodes 过滤或 path+siblings）、点击 `focusNode`。当前 focus 高亮。可折叠宽度（按钮 toggle，class 在 shell）。

- [ ] **Step 2: RightGraph**  
  SVG 渲染 `layoutGraph(nodes)`；边 + 圆点；`focusId` 实心；`unread` 小点；click → `focusNode`。

- [ ] **Step 3: 手动** `npm run dev`：点列表与图，标题/focus 应变（若中卡仍占位，至少 store focus 变；若 Plan04 已合入则整卡变）。

- [ ] **Step 4: Commit**

```bash
git add src/components/shell
git commit -m "feat: left rail and SVG inquiry graph"
```

---

## Acceptance

- [ ] vitest 分叉/重生测试通过
- [ ] 左栏与图可换焦
- [ ] 未改 card/overlays 目录业务（除必要）
- [ ] ≥1 commit
