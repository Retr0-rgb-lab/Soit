# Plan N2: McpState registry + CLI 接线

> **For agentic workers:** mod.rs 重构 + main.rs 重写,一个编译单元。0.4d。依赖 N1。
> **Spec:** `docs/superpowers/specs/2026-08-25-mcp-multi-workspace-spec.md` §2.1 / 2.2(v1.1)
> **工作目录:** `/home/peleclic/workspace/soit`

---

### Task 2.1: mod.rs McpState

**Files:**
- Modify: `src-tauri/src/mcp/mod.rs`

- [ ] **Step 1: 读现状(签名、handle_line、dispatch、现有测试)**
```bash
sed -n '1,120p' src-tauri/src/mcp/mod.rs
```

- [ ] **Step 2: 新结构**

```rust
pub struct McpServeConfig {
  pub registry: Vec<PathBuf>,     // 白名单(显式在前,总量 ≤8)
  pub allow_any: bool,
  pub default: Option<PathBuf>,   // 首个显式 --vault 或 lastVault
}

pub struct McpState {
  open: std::collections::HashMap<PathBuf, Universe>, // 惰性打开缓存
  allowlist: Vec<PathBuf>,                            // canonicalize 后的白名单
  allow_any: bool,
  selected: Option<PathBuf>,                          // canonicalize 后
}

pub fn run_stdio_serve(config: McpServeConfig) -> Result<(), String>
```

- **白名单匹配安全(v1.1)**:启动时把显式 `--vault` 逐条 `dunce::canonicalize`(失败 → 启动错误);recents 条目**不提前 IO**,命中比较时才 canonicalize;请求 vault(工具参数 / select 参数)比较前同样 canonicalize,失败 → 拒绝。防 `..`/symlink 逃逸 + Windows 大小写不一致。

- [ ] **Step 3: resolve_vault**

```rust
impl McpState {
  /// 参数 vault > selected > 白名单唯一库 > 错误「多工作区未选择,先 list_workspaces / select_workspace」
  fn resolve_vault(&mut self, args: Option<&Value>) -> Result<&mut Universe, String> {
    // 1. arg_str(args, "vault"): canonicalize → 白名单/allow_any 校验 → open_or_cached
    // 2. self.selected: 同上
    // 3. allowlist.len()==1: 用它
    // 4. Err(提示 list_workspaces)
    // open 失败错误带 vault 路径 + 原始错误;失败后不缓存
  }
  fn open_or_cached(&mut self, canon: PathBuf) -> Result<&mut Universe, String> {
    // entry(canon).or_insert_with(|| Universe::open_readonly(...))
    // 注意:or_insert_with 闭包不能返回 Result —— 用 match entry 手写
  }
  fn is_allowed(&self, canon: &Path) -> bool { self.allow_any || self.allowlist.iter().any(...) }
}
```

- [ ] **Step 4: handle_line / dispatch 签名改 `&mut McpState`**

```rust
fn handle_line(line: &str, state: &mut McpState) -> Option<Value>
fn dispatch(method, params, id, state: &mut McpState) -> Value
// tools/call 分发:tools::call_tool(name, args, state) —— N3 会改 tools.rs 签名,
// 本阶段先以 state.resolve_vault(args) 取出 universe 再传旧签名?不:
// 直接把 call_tool 的 &Universe 参数改为 &mut McpState(本 commit 同时小改 tools.rs
// 的函数头,body 内部分发留 N3)。保证编译通过。
```

- [ ] **Step 5: 现有测试重写**(`dispatch_initialize_tools_and_call` 等:构造 `McpState` 替代 `&Universe`;断言 tools/list 数量改为 **7**)

### Task 2.2: main.rs CLI

**Files:**
- Modify: `src-tauri/src/main.rs`

- [ ] **Step 6: 重写 run_mcp**(spec §2.1 v1.1)

```rust
// 解析:serve 令牌;--vault 重复/逗号;--allow-any;未知参数 exit(2)
// 注册表 = 显式(保序去重不截断)+ recents(read_session_raw_no_app → migrate → normalize_recent_vaults)去重补齐,cap 8
// 显式路径必须绝对;相对 → eprintln + exit(2)
// default = 显式第一个,否则 last_vault(在注册表内才生效),否则 None
// run_stdio_serve(McpServeConfig { registry, allow_any, default })
```

- [ ] **Step 7: 编译验证 + commit**

```bash
# cargo check 卡 libdbus 为已知环境问题;用 `rustc --edition 2021` 不行(依赖 crate)。
# 策略:人工核对 + 语义走查;把 cargo 编译验证留给 Windows。
# 若想更稳:搭临时 stub crate 跑 resolve_vault 单测(参考 read-ergonomics 的 stub 经验)。
cd /home/peleclic/workspace/soit && git add src-tauri/src/mcp/mod.rs src-tauri/src/main.rs src-tauri/src/mcp/tools.rs
git commit -m "feat(mcp): multi-workspace registry, lazy open, select state, CLI"
```

---

## Acceptance

- [ ] `McpServeConfig` / `McpState` / `resolve_vault` 落地;白名单比较双向 canonicalize
- [ ] dispatch/handle_line/tools 分发全部 `&mut McpState`;编译语义正确(待 Windows cargo test)
- [ ] mod.rs 现有测试改签名后逻辑仍对;tools/list 断言 7
- [ ] main.rs:单 `--vault` 旧行为等价;重复/逗号/无 vault/allow-any/相对路径拒绝
- [ ] 1 个 commit
