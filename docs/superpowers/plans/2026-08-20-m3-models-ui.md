# Plan M3: 可用模型 UI + Composer chip

> **For agentic workers:** Wave 2 after M2（或 M1+M2 已合）  
> **Spec:** `docs/superpowers/specs/2026-08-20-model-providers-spec.md` §2.2.2 §2.3  
> **工作目录:** `E:\学习软件\Soit`  
> **Owns:** `ModelsPanel.tsx`, `ModelForm.tsx`, `ModelSettingsForm.tsx` models tab slot, `Composer.tsx` chip label  
> **Do not touch:** ProvidersPanel internals except shared types; Rust unless needed

---

### Task M3.1: ModelsPanel + ModelForm

**Files:**
- Create: `src/components/shell/settings/ModelsPanel.tsx`
- Create: `src/components/shell/settings/ModelForm.tsx`
- Modify: `ModelSettingsForm.tsx` wire models tab
- Modify: `settings.css`

- [ ] **Step 1:** ModelForm: provider select*, modelId*, optional label
- [ ] **Step 2:** ModelsPanel: empty guides; list with provider name, active badge, enable toggle, set-active, edit/delete
- [ ] **Step 3:** set active → setModelSettings + `soit:chat-config-changed`
- [ ] **Step 4:** No provider → CTA to switch tab (callback `onNeedProviders`)
- [ ] **Step 5: Commit**
```bash
git add src/components/shell/settings/ModelsPanel.tsx src/components/shell/settings/ModelForm.tsx src/components/shell/settings/ModelSettingsForm.tsx src/components/shell/settings/settings.css
git commit -m "feat(settings): models catalog and active conversation model"
```

---

### Task M3.2: Composer chip reflects active model

**Files:**
- Modify: `src/components/card/Composer.tsx` (or wherever chip loads config)

- [ ] **Step 1:** On config load / `soit:chat-config-changed`, show `Mock · 本地` or `在线 · {label|modelId}`
- [ ] **Step 2:** Prefer getModelSettings resolve display name when available
- [ ] **Step 3: Commit**
```bash
git add src/components/card/Composer.tsx
git commit -m "feat(composer): chip shows active model display name"
```

---

## Acceptance

- [ ] Add model under provider; set as conversation model
- [ ] Chip updates without reload
- [ ] Disable/delete active → falls back Mock
- [ ] 2 commits
