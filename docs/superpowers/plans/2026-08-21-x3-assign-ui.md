# Plan X3: 设置 · 模型 · 分配 Tab

> **For agentic workers:** Wave 2 并行 X2；依赖 X1 字段已存在；不改 explainActions / chat_config  
> **Spec:** `docs/superpowers/specs/2026-08-21-model-assignment-spec.md` v1.1 §2.4  
> **工作目录:** `E:\学习软件\Soit`  
> **Owns:** `src/components/shell/settings/ModelSettingsForm.tsx`, `AssignmentPanel.tsx`（新建）, `settings.css`（少量）  
> **Do not touch:** `modelSettings.ts`, `explainActions.ts`, `Composer.tsx`, `ProvidersPanel.tsx`, `ModelsPanel.tsx`（normalize 写盘已清非法槽）, `workspaceStore*`

**前置：** `ModelSettings.explainModelId` 已在类型里。若 X1 未完成则停。

---

### Task X3.1: AssignmentPanel

**Files:**
- Create: `src/components/shell/settings/AssignmentPanel.tsx`
- Modify: `src/components/shell/settings/settings.css`

- [ ] **Step 1:** 面板加载 `getModelSettings()`。`enabled = settings.models.filter(m => m.enabled)`。展示名：`label?.trim() || modelId`。

- [ ] **Step 2:** 无启用模型 → 空态「请先在可用模型中添加并启用。」+ 按钮 `前往可用模型` → `onNeedModels()`。

- [ ] **Step 3:** 两行 `settings-field`：

```tsx
<label className="settings-field">
  <span>对话模型</span>
  <select
    value={settings.activeModelId ?? ""}
    onChange={(e) => void persist({
      ...settings,
      activeModelId: e.target.value ? e.target.value : null,
    })}
  >
    <option value="">Mock（未选用）</option>
    {enabled.map((m) => (
      <option key={m.id} value={m.id}>{displayName(m)}</option>
    ))}
  </select>
  <span className="settings-model-note">卡片作曲与发消息。也可在作曲条切换。</span>
</label>

<label className="settings-field">
  <span>短解释模型</span>
  <select
    value={settings.explainModelId ?? ""}
    onChange={(e) => void persist({
      ...settings,
      explainModelId: e.target.value ? e.target.value : null,
    })}
  >
    <option value="">跟随对话</option>
    {enabled.map((m) => (
      <option key={m.id} value={m.id}>{displayName(m)}</option>
    ))}
  </select>
  <span className="settings-model-note">点词 / 划词浮层。建议用更快更便宜的模型。</span>
</label>
```

`persist` = `setModelSettings` + `soit:chat-config-changed`（与 ModelsPanel 相同）。

- [ ] **Step 4:** 可选 P1：若选中的短解释模型其供应商 `!apiKey.trim()`，在 note 下加一句「该供应商未配置密钥，短解释将跟随对话。」不挡保存、不清槽。

- [ ] **Step 5:** CSS 最多加 `.settings-assign` gap；复用 `settings-field` / `settings-model-note`。暖纸 token，不抄 Explore 黄粉，无倍率/档位徽章。

- [ ] **Step 6: Commit**
```bash
git add src/components/shell/settings/AssignmentPanel.tsx src/components/shell/settings/settings.css
git commit -m "feat(settings): assignment panel for chat vs explain slots"
```

---

### Task X3.2: ModelSettingsForm 第三 Tab

**Files:**
- Modify: `src/components/shell/settings/ModelSettingsForm.tsx`

- [ ] **Step 1:** `type ModelTab = "providers" | "models" | "assign"`。默认 Tab 逻辑不变：无供应商 → providers，否则 models（**不要**默认 assign）。

- [ ] **Step 2:** 子 nav 第三钮「分配」。intro：

```text
本机 BYOK：先添加供应商凭证，再在可用模型中建目录，然后在分配里把模型绑到对话或短解释。密钥不进宇宙库。
```

- [ ] **Step 3:** tabpanel：`assign` → `<AssignmentPanel onNeedModels={() => setTab("models")} />`。

- [ ] **Step 4: Commit**
```bash
git add src/components/shell/settings/ModelSettingsForm.tsx
git commit -m "feat(settings): add model assignment sub-tab"
```

---

## Acceptance

- [ ] 三个子 Tab；默认仍是供应商或可用模型
- [ ] 「跟随对话」value="" → `explainModelId: null`，刷新仍在
- [ ] 改短解释槽不改 Composer 对话 chip（只 dispatch 事件；chip 仍读 `activeModelId`）
- [ ] 无套餐/倍率/视觉/拖拽列表
- [ ] 2 个 commit
