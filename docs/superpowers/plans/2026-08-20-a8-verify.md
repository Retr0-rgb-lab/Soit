# Plan A8: 全量验收（mock 闭环）

> **For agentic workers:** verify only; fix blockers; no new features  
> **Spec:** v1.1 §6  
> **工作目录:** `E:\学习软件\Soit`  
> **Wave:** 5

---

### Task 8.1: Automated

```bash
npm test
npm run build
cd src-tauri && cargo test && cargo check
```

Fix any failures with minimal diffs; commit fixes separately if needed.

### Task 8.2: Checklist vs Spec §6

Mark each acceptance item in the spec file or a short `docs/superpowers/plans/2026-08-20-a8-verify-log.md`:

- dual-track 共识
- runCompletion shared
- cancel / empty text
- inflight mutex
- cardBrief no parent turns
- export/import
- list_runtimes mock + enableSpawn false
- mock handoff nodes unchanged
- no shell plugin
- settings runtime section
- cold start clean
- LOC caps

### Task 8.3: Commit docs if checklist added

```bash
git add docs/superpowers/
git commit -m "docs(agent): dual-track verify log and plans"
```

---

## Acceptance

- [ ] All automated commands green
- [ ] Spec §6 checklist recorded
- [ ] Ready to push branch
