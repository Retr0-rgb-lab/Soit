# Plan X4: AGENTS 指针 + 回归

> **For agentic workers:** Wave 3；X2+X3 完成后再跑；可改文档与测，不新开功能  
> **Spec:** `docs/superpowers/specs/2026-08-21-model-assignment-spec.md` v1.1 §2.5 §6  
> **工作目录:** `E:\学习软件\Soit`  
> **Owns:** nested AGENTS, old specs pointers, verify commands  
> **Do not touch:** 新功能代码，除非测红且属于本 Spec 回归（修要最小）

**前置：** X1–X3 已 commit。`explainSpan` 已用 `resolveExplainPort`；设置有「分配」Tab。

---

### Task X4.1: 文档指针

**Files:**
- Modify: `docs/superpowers/specs/2026-08-20-model-providers-spec.md` §7
- Modify: `docs/superpowers/specs/2026-08-20-card-read-explain-spec.md` 前言
- Modify: `src/lib/AGENTS.md`
- Modify: `src/components/shell/AGENTS.md`
- Modify: `src/state/AGENTS.md`（若有 explain 句则改；否则在 Rules 加一句 `explainSpan` → `resolveExplainPort`）
- Modify: `src-tauri/AGENTS.md`（`chat_config.rs` 行：ModelSettings 含 `explainModelId`）
- **不要**创建 `src/lib/chat/AGENTS.md`

- [ ] **Step 1:** model-providers §7「对话/功能/视觉 **多槽分配**（P2…）」改为：对话/短解释槽见 `2026-08-21-model-assignment-spec.md` v1.1；**视觉槽仍后置**。

- [ ] **Step 2:** card-read-explain 前言「复用 `resolvePort`」改为短解释走 `resolveExplainPort`（`explainModelId`，缺省跟随对话）；§2.2 入口仍是 `explainSpan`。

- [ ] **Step 3:** `src/lib/AGENTS.md` ModelSettings 行：shape 含 `explainModelId`；`resolveExplainPort`；**explain 禁止** `resolvePort`。Spec 指针加上 assignment spec。

- [ ] **Step 4:** `src/components/shell/AGENTS.md` 模型子 Tab：**供应商 · 可用模型 · 分配**。分配 = 对话 `activeModelId` + 短解释 `explainModelId`（null=跟随）。

- [ ] **Step 5:** `src/state/AGENTS.md`：`explainSpan` 经 `resolveExplainPort`，不写 db。

- [ ] **Step 6:** `src-tauri/AGENTS.md`：`soit-chat.json` ModelSettings 含可选 `explainModelId`；`get_chat_config` 仍只投影对话槽。

- [ ] **Step 7: Commit**
```bash
git add docs/superpowers/specs/2026-08-20-model-providers-spec.md docs/superpowers/specs/2026-08-20-card-read-explain-spec.md src/lib/AGENTS.md src/components/shell/AGENTS.md src/state/AGENTS.md src-tauri/AGENTS.md
git commit -m "docs: point AGENTS and specs at chat vs explain slots"
```

若 `知识库/specs/2026-08-20-model-providers-spec.md` 是副本且 §7 仍写 P2，同样改一句并纳入本 commit。

---

### Task X4.2: 测试与构建

- [ ] **Step 1:** `npx vitest run src/lib/chat/modelSettings.test.ts src/state/explainActions.test.ts`
- [ ] **Step 2:** `npm test`
- [ ] **Step 3:** `npm run build`
- [ ] **Step 4:** `cd src-tauri && cargo test chat_config -- --nocapture`（或 `cargo test` 若很快）
- [ ] **Step 5:** 抽检：`explainActions.ts` 无 `resolvePort`；`ModelSettingsForm` 有「分配」；`upsertFromChatConfig` 拷贝 `explainModelId`。
- [ ] **Step 6:** 若红：最小修复后 **另 commit**，message 说明回归点。不要顺手做视觉槽。

---

## Acceptance

- [ ] Spec §6.1–6.4 可在代码里勾
- [ ] `npm test` / `npm run build` / cargo chat_config 绿
- [ ] 旧 spec 不再说「多槽整包 P2」而漏本波
- [ ] ≥1 个文档 commit；修复 commit 按需
