# Plan K3: MdTextView math + doc.css

> **Depends on K1 merged**  
> **Spec:** v1.1 §2.5 §2.6  
> **工作目录:** `E:\学习软件\Soit`  
> **可写:** `src/components/doc/MdTextView.tsx`, `src/components/doc/doc.css`, optional small pure export test colocated  
> **禁止:** `assistantHtml.ts`, `port.ts`, `package.json`, `src/lib/math/**` (import only)  
> **并行:** 与 K2 并行

---

### Task 3.1 MdTextView pipeline

After code placeholders, call `protectAndRenderMath(s, put)` with **same** PH alphabet `\uE000`/`\uE001` as today in MdTextView.

### Task 3.2 doc.css

```css
.md-text-view .soit-math-inline { … }
.md-text-view .soit-math-block { overflow-x: auto; text-align: center; … }
.md-text-view .soit-math-fallback { … }
.md-text-view .katex { color: inherit; }
```

### Task 3.3 Optional unit test

If `renderDocMd` is not exported, export for test or test via thin `renderDocMd` export in same file.

At least one test: `renderDocMd`/`renderDoc` with `$x$` yields katex class.

### Task 3.4 Commit

```bash
npm test -- # relevant
npm run build
git commit -m "feat(doc): KaTeX in materials md preview (K3)"
```

## Acceptance
- [ ] md preview renders `$` / `$$`  
- [ ] 1 commit  
